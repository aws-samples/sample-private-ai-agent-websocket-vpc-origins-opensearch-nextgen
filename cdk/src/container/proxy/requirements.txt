# Production runtime dependencies for the WebSocket<->SSE proxy container (v2).

# Web framework + ASGI server (native async + WebSocket support)
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0
websockets>=14.0,<16.0
# Multipart form parsing for the document-upload endpoint (/api/upload).
python-multipart>=0.0.9,<0.1.0

# AWS SDK — invokes bedrock-agentcore:InvokeAgentRuntime over PrivateLink,
# stores uploads in S3, embeds with Bedrock, and indexes into OpenSearch.
boto3>=1.40.0,<2.0.0
botocore>=1.40.0,<2.0.0

# Cognito Hosted-UI auth: validate the ID/access JWT against the pool JWKS.
python-jose[cryptography]>=3.3.0,<4.0.0

# Document parsing for the live-audit upload feature.
pypdf>=4.2.0,<6.0.0
python-docx>=1.1.0,<2.0.0

# OpenSearch Serverless ingestion of uploaded documents (RAG persistence).
opensearch-py>=2.6.0,<3.0.0
requests-aws4auth>=1.2.3,<2.0.0
