# Development / test-only dependencies (not installed in the production image).
-r requirements.txt

# Test runner + property-based testing
pytest>=9.0.3,<10.0.0
pytest-asyncio>=0.23.0,<1.0.0
hypothesis>=6.100.0,<7.0.0

# FastAPI TestClient transport (Starlette TestClient depends on httpx)
httpx>=0.27.0,<1.0.0
